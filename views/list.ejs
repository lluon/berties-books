<!doctype html>
<html>
  <head>
    <title> <%=shopData.shopName%> - book list </title>
    <link rel="stylesheet" type="text/css" href="/main.css">
  </head>
  <body>
    <h1>X<%=shopData.shopName%> - Book List</h1>
    <h2>here are the books that we sell </h2>
      <ul>
        <% for (let i=0; i<availableBooks.length; i++){ %>
        <li>
          <%= availableBooks[i].name %>,
          £<%= Number(availableBooks[i].price).toFixed(2) %>
        </li>
          <% } %>
      </ul>
  </body>
</html>